services:
  www:
    build: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - type: bind
        source: ./ssl
        target: /usr/local/apache2/ssl
      - type: bind
        source: ./.well-known
        target: /usr/local/apache2/htdocs/.well-known
    restart: always
